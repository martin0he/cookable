import { Typography } from "@mui/material";
import PageLayout from "./PageLayout";

const NotFound = () => {
  return (
    <PageLayout>
      <Typography variant="h1">Page Doesn't Exist :(</Typography>
    </PageLayout>
  );
};

export default NotFound;
